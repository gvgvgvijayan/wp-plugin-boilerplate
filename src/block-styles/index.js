/**
 * Block styles entry point.
 *
 * Import custom block styles here. This file is the webpack entry for the
 * `block-styles` build. Add your block style registrations, e.g.:
 *
 *   import './styles/my-custom-style.scss';
 *   wp.domReady( () => {
 *       wp.blocks.registerBlockStyle( 'core/group', {
 *           name: 'my-style',
 *           label: 'My Style',
 *       } );
 *   } );
 */

// Placeholder import so the entry resolves and produces an output asset.
import './placeholder.scss';
